# ============================================================
# 版本锁定 — 确保教学项目长期稳定运行
# 更新日期: 2026-05-29
# ============================================================
#
# 安装方式:
#   pip install -r requirements.txt --break-system-packages
#
# 依赖按引入阶段分组，与项目演进路线对应:
#
#   app/   ──┐
#            ├─ 阶段 1: Web 基础
#   app-1/ ─┘
#                │
#   app1/  ──────┘  阶段 2: 架构重构（无新依赖）
#                │
#   app3/  ──────┘  阶段 3: 认证
#                │
#   app4/  ──────┘  阶段 4: 测试
#
# ============================================================

# ---- 阶段 1: Web 基础 (app/, app-1/, app1/) ----
fastapi==0.115.0          # Web 框架核心
uvicorn[standard]==0.32.0 # ASGI 服务器
sqlalchemy==2.0.36        # ORM + 数据库
pydantic==2.9.2           # 请求/响应数据验证
jinja2==3.1.2             # 模板引擎 (HTMX 渲染)
starlette==0.38.6         # ASGI 工具集 (FastAPI 底层)
python-multipart==0.0.29  # 表单数据解析 (app-1/ HTMX 表单需要)

# ---- 阶段 3: 认证 (app3/, app4/) ----
python-jose[cryptography]==3.5.0 # JWT 生成/验证
passlib[bcrypt]==1.7.4           # 密码哈希
bcrypt==4.0.1                    # bcrypt 算法后端

# ---- 阶段 4: 测试 (app4/) ----
pytest==9.0.3            # 测试框架
httpx==0.28.1            # HTTP 客户端 (TestClient 依赖)

# ============================================================
# 注意: 不要随意升级版本!
# 如需升级, 请先在测试环境验证所有功能正常
# ============================================================

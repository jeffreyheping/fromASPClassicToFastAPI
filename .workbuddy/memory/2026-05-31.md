# 2026-05-31

## app-5 创建 — 路由分离版
- **演进位置**：app-6 → **app-5** → app-2 → app-1 → app → app+1
- **核心变化**：SQL 从 Mako 模板抽到 `routers/todos.py`，模板换 Jinja2，保持 raw sqlite3
- **不变**：和 app-6 用完全相同的 SQL 语句（INSERT/UPDATE/DELETE/TOGGLE 逻辑一致）
- **从 app-2 复制**：templates/（index.html + edit.html）和 static/css/style.css 一字不改
- **教学点**：「关注点分离」——SQL 归路由，HTML 归模板，各司其职
- **设计心机**：`database.py` 的 `get_db()` 用 `yield` 生成器，签名和 app-2 的 SQLAlchemy 版本一致，到了 app-2 路由代码几乎不用动
- 端口 8005，数据库 todo-5.db
- 已验证：首页 200、CSS 200、新增 303 重定向、列表渲染正确

## 演进路线全貌
```
app-6 → app-5 → app-2 → app-1 → app → app+1
 模板    路由    ORM    HTMX   Vue   架构分离
 即路由  分离   抽象    局部刷新 SPA
```
每一步只变一个维度，连贯性好。

## app-5 README 重写
- 参照 app-6 和 app-2 的风格，扩充为完整教学文档
- 新增三栏对比表（app-6 vs app-5 vs app-2）
- 新增「核心代码解读」：database.py 依赖注入、routers/todos.py SQL 归宿、模板只做渲染
- 新增 SQL 对照表（6 条 SQL，标注从 app-6 原样搬来，到 app-2 换 ORM）
- 保留路由一览、项目结构、快速开始、演进路线

## 根 README 全面重写（6 项目版）
- 摸底全部 6 个项目（app-6/5/2/1/app/app+1）后重写
- 演进路线更新为完成态：app-6 → app-5 → app-2 → app-1 → app → app+1
- 新增六版本对比表（12 个维度：模板引擎、数据库、前端技术、刷新方式、Pydantic、CORS、进程数、端口、数据库文件、模板文件数、入口文件）
- 新增「每一步详解」：6 段，每段包含核心变化 + 教学价值
- 技术栈按引入阶段分组（第 1-2 步 / 第 3-4 步 / 第 5 步 / 第 6 步）
- 快速开始补全 app-5 启动命令
- 移除所有 app1/app3/app4 引用，统一用废弃标注说明
- 修复 app+1 端口（8010+5173）

# 2026-06-01

## 6 版本 UI 统一

- 所有版本统一标题格式：`📋 待办事项（版本描述）`
- 统一样式：`<p class="subtitle">` 一行介绍 + `<div class="info-box">` 详细原理说明
- 每个 info-box 含向前/向后版本链接
- CSS 统一添加 `.subtitle` 和 `.info-box` 样式（.subtitle: #666, 14px; .info-box: #e8f4f8 背景）
- h1 margin-bottom 从 20px 缩到 10px 为 subtitle 留空间

### 各版本 subtitle
| 版本 | subtitle |
|------|----------|
| app-6 | Python + SQL + HTML 全写在一个文件——最原始也最直观的 Web 开发 |
| app-5 | SQL 从模板抽到路由函数——关注点分离的第一步 |
| app-2 | 每个操作都整页刷新——ASP Classic 时代的真实体验 |
| app-1 | 不写一行 JavaScript 的 AJAX——用 HTML 属性实现局部刷新 |
| app/ | Vue 3 接管前端，前后端通过 JSON API 通信 |
| app+1 | 告别 CDN —— 用 Vite 构建工具管理前端工程 |

### app/ 模板重构
- 从多容器 (`v-if="view==='list'" class="container"`) 改为单容器 + 条件内容
- 主 H1 始终可见，add/edit 视图改用 `<h2>`
- info-box 放在 `#app` 外部，纯静态 HTML

### app+1 info-box
- 放在 `.container` 外部但在 `<template>` 内

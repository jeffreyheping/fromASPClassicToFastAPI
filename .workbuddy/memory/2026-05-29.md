# 2026-05-29

## app3 研读
- 从 GitHub 拉取 app3 代码，完整研读了全部文件
- app3 在 app2 基础上做了 OAuth2 标准化升级：Vue 端登录改用 `/api/auth/token` + `OAuth2PasswordRequestForm`（标准 OAuth2 密码流）
- 发现并修复 bug：app3/static/js/app.js 中 toggle 路径写的是 `/done`，后端是 `/toggle`，已修复为 `/toggle`
- app3 的 import 方式仍为绝对导入（`from core.xxx`），已改造为相对导入：main.py→`.core.xxx`，routers→`...core.xxx`
- app3/__init__.py 有注释笔误（写的是 "app2"），已修复为 "app3"
- 清理了旧的 __pycache__ 避免缓存残留
- 对比 fastapi-users：当前手写方案对简单认证需求更简洁，fastapi-users 的优势在邮箱验证/OAuth/密码重置等高级功能
- app3 启动命令：`uvicorn app3.main:app --host 0.0.0.0 --port 8007 --reload`（数据库 todo3.db）
- 重写 app3/README.md：移除了所有 app2 对比引用，改为独立文档，端口统一为 8007
- 演进路线简化为 app1 → app3 → 生产环境

## app4 — 复制 + 测试

- 从 app3 复制出 app4，改名 todo4.db、port 8008
- 最小改动以支持测试：
  - `config.py`：os.getenv 支持环境变量覆盖（APP4_DATABASE_URL, APP4_SECRET_KEY）
  - `database.py`：改用 DATABASE_URL 替代 DB_URI
  - `security.py`：SECRET_KEY 从 config 读取，不再硬编码
  - `main.py`：改为 create_app() 工厂函数，建表逻辑移出工厂
- 编写 23 个 pytest 测试：
  - test_services.py（9 项）：纯业务逻辑 CRUD
  - test_api_auth.py（6 项）：注册/登录/密码验证
  - test_api_todos.py（8 项）：认证拦截 + CRUD 全流程
- 测试使用 in-memory SQLite + StaticPool + dependency_overrides
- 全部 23 个测试通过
- 更新 README：演进路线 app1→app3→app4

## 根目录 README 重构
- 原 README 仅覆盖 app/ 和 app-1/，完全缺失 app1/app3/app4
- 重写为五个版本的完整演进路线：app/ → app-1/ → app1/ → app3/ → app4/
- 五个版本对比表（架构、前端、认证、测试、端口、数据库）
- 每个阶段的「核心变化」和「教学价值」
- 移除过时的 `todo_fastapi_one_1/` 目录引用
